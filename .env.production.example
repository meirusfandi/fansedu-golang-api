# Production — set these in your host/container (do not commit .env in prod)
ENV=production
PORT=8080
# Required in production
DATABASE_URL=postgres://user:pass@host:5432/fansedu?sslmode=require
# Required: use a strong random secret, e.g. openssl rand -base64 32
JWT_SECRET=
